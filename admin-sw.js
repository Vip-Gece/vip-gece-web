"use strict";const ADMIN_PATH_PREFIXES=["/vg-panel-91x","/admin.css","/admin.js","/config.js","/public/js/admin/","/api/admin/","/api/v1/admin/"];function isAdminRequest(request){try{const url=new URL(request.url);if(url.origin!==self.location.origin)return false;return ADMIN_PATH_PREFIXES.some(prefix=>url.pathname===prefix||url.pathname.startsWith(prefix))}catch{return false}}self.addEventListener("install",event=>{event.waitUntil(self.skipWaiting())});self.addEventListener("activate",event=>{event.waitUntil(self.clients.claim())});self.addEventListener("fetch",event=>{const{request:request}=event;if(request.method!=="GET"||!isAdminRequest(request)){event.respondWith(fetch(request));return}event.respondWith(fetch(request,{cache:"no-store"}))});
